# NexBox 开机自启动方案

## 问题背景

之前实现了一版开机自启动功能（`src-tauri/src/auto_start.rs`），注册表写入了，任务管理器里也能看到，但**开机后并没有真正启动**。

## 问题根因分析

对现有代码逐行分析，发现以下致命缺陷：

### 1. 路径未加引号（最关键！）

```rust
// 当前代码 - 有 BUG
let app_path = std::env::current_exe()?
    .to_string_lossy()
    .replace("/", "\\");  // ❌ 没有 enclosing quotes

// 写入注册表的值: C:\Program Files\NexBox\nexbox.exe
// Windows 解析时会在空格处截断，找不到 exe！
```

当程序安装路径包含空格（如 `C:\Program Files\NexBox\nexbox.exe`），Windows 在执行 Run 键值时会在第一个空格处截断路径，导致找不到可执行文件。**这是"注册表写了但不启动"的头号原因。**

### 2. 仅使用单一注册表方案（无冗余）

只写了 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`，该方案存在多个失效场景：

| 失效场景 | 说明 |
|---------|------|
| 任务管理器禁用 | 用户或某软件在任务管理器"启动"选项卡中禁用了该项目（注册表值仍在但被标记为禁用） |
| 组策略限制 | 企业环境通过 GPO 禁止 Run 键启动项 |
| 安全软件拦截 | 某些杀毒软件会阻止注册表自启项 |
| 路径变更 | 程序更新/迁移后注册表路径失效 |

### 3. 检查状态不准确

```rust
// 当前代码 - 只检查注册表值是否存在
let output = exec_reg(&["query", REG_PATH, "/v", APP_NAME])?;
Ok(output.status.success())  // ❌ 即使被任务管理器禁用也会返回 true
```

注册表值存在 ≠ 自启已启用。Windows 会在 `HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\StartupApproved\Run` 下写入启用/禁用状态，现有代码完全没有检查这个。

## 推荐方案：注册表 + 启动文件夹双写

### 方案对比

| 方案 | 稳定性 | 实现复杂度 | 任务管理器可见 | 抗禁用 | 推荐 |
|------|--------|-----------|--------------|--------|------|
| ❌ 仅 HKCU Run（当前） | 低 | 低 | 是 | 弱 | 不推荐 |
| ✅ HKCU Run（修复引号） | 中 | 低 | 是 | 中 | 作为基础 |
| ✅ 启动文件夹 .lnk 快捷方式 | 高 | 中 | 是 | 强 | 作为主方案 |
| ❌ HKLM Run | 中 | 需管理员 | 是 | 中 | 权限过高 |
| ❌ 任务计划程序 | 最高 | 高 | 否 | 最强 | 过重，不推荐 |

### 最终方案：启动文件夹快捷方式（主） + 注册表（辅）

**核心思路：**
1. **主方案**：在用户启动文件夹创建 `.lnk` 快捷方式 → 稳定性最高，用户可见可管理
2. **辅方案**：同时写入注册表 Run 键（路径加引号）→ 兼容任务管理器管理习惯
3. **状态检查**：综合检查两种方式 + 启用状态
4. **冗余清理**：关闭时同时清除两处

### 为什么选择启动文件夹快捷方式作为主方案

- **成功率最高**：Windows 原生机制，不受注册表策略限制
- **用户透明**：用户可以在文件管理器中直接看到、删除
- **任务管理器兼容**：任务管理器"启动"选项卡同样会列出启动文件夹的项目
- **支持参数**：可以通过快捷方式传递启动参数
- **不受注册表禁用影响**：即使注册表 Run 项被禁用，启动文件夹快捷方式仍有效

## 技术实现细节

### Rust 后端（`auto_start.rs`）

#### 1. 创建快捷方式（使用 PowerShell COM 接口）

```rust
// 通过 PowerShell 调用 WScript.Shell 创建 .lnk 文件
fn create_startup_shortcut(exe_path: &str) -> Result<(), String> {
    let startup_dir = dirs::config_dir()
        .map(|p| p.join("Microsoft").join("Windows").join("Start Menu").join("Programs").join("Startup"))
        .ok_or("无法获取启动文件夹路径")?;

    let lnk_path = startup_dir.join("NexBox.lnk");
    
    let ps_script = format!(
        r#"$ws = New-Object -ComObject WScript.Shell;
        $s = $ws.CreateShortcut('{}');
        $s.TargetPath = '{}';
        $s.WorkingDirectory = '{}';
        $s.Save()"#,
        lnk_path.to_string_lossy().replace("'", "''"),
        exe_path.replace("'", "''"),
        parent_dir.replace("'", "''")
    );
    // 执行 PowerShell...
}
```

#### 2. 注册表写入（修复引号问题）

```rust
// 路径用双引号包裹
let quoted_path = format!("\"{}\"", app_path);
exec_reg(&["add", REG_PATH, "/v", APP_NAME, "/t", "REG_SZ", "/d", &quoted_path, "/f"])?;
```

#### 3. 综合状态检查

```rust
pub async fn check_nexbox_auto_start() -> Result<bool, String> {
    // 检查注册表值是否存在
    let reg_exists = check_registry_value();
    // 检查启动文件夹快捷方式是否存在
    let shortcut_exists = check_startup_shortcut();
    // 任一存在即视为已启用
    Ok(reg_exists || shortcut_exists)
}
```

### 前端（`SettingsPage.tsx`）

- 取消注释开机自启 UI 区块
- 放在通用设置的**第一个**位置
- 添加加载状态和错误提示
- 保持与现有 ThemeSwitch 组件一致的交互体验

## Todolist

- [ ] 1. 重写 `src-tauri/src/auto_start.rs`
  - [ ] 1.1 修复注册表路径引号问题（`"path"` 包裹）
  - [ ] 1.2 新增启动文件夹快捷方式创建/删除逻辑
  - [ ] 1.3 改进 `check_nexbox_auto_start` 综合检查注册表 + 快捷方式
  - [ ] 1.4 添加详细日志输出（路径、成功/失败原因）
  - [ ] 1.5 处理 PowerShell 执行失败的 fallback
- [ ] 2. 前端 `SettingsPage.tsx` 修改
  - [ ] 2.1 取消注释开机自启 UI 区块，放在通用设置第一个
  - [ ] 2.2 完善错误提示（toast 通知写入失败）
  - [ ] 2.3 添加描述文案说明双写机制
- [ ] 3. 多语言文案完善
  - [ ] 3.1 检查 `zh.json` / `zh-TW.json` / `en.json` / `ja.json` / `fr.json` / `de.json` 的 autoStartLabel 和 autoStartDesc
  - [ ] 3.2 更新描述文案为更准确的说明
- [ ] 4. 测试验证
  - [ ] 4.1 开发环境测试开关功能正常
  - [ ] 4.2 验证注册表写入路径正确（含引号）
  - [ ] 4.3 验证启动文件夹快捷方式创建成功
  - [ ] 4.4 重启电脑验证实际自启效果
  - [ ] 4.5 测试关闭自启后两处均被清除
  - [ ] 4.6 测试路径含空格的场景
