# 自定义页面（Custom Dashboard）开发计划

## 一、需求概述

新增一个「自定义页面」，允许用户将常用工具/页面以卡片形式自由摆放到一个画布上，支持拖动、改变大小、改变圆角。页面通过导航栏入口进入，**默认关闭**，可在「设置 → 通用 → 导航栏」中开启。同时为导航栏按钮增加拖拽排序功能（参考快捷启动 `GameLauncher` 的 `@dnd-kit` 实现）。

### 核心功能清单

| # | 功能 | 说明 |
|---|------|------|
| 1 | 自定义页面 `CustomPage` | 自由布局画布，放置工具卡片 |
| 2 | 导航栏入口 | 在 `sidebar.tsx` 的 `navItems` 中新增条目，默认隐藏 |
| 3 | 添加工具弹窗 | 平铺所有可添加项的卡片（图标+名称），排除第三方工具 |
| 4 | 卡片拖动 | 自由拖动改变位置 |
| 5 | 卡片改变大小 | 拖拽右下角/边缘调整尺寸 |
| 6 | 卡片改变圆角 | 每个卡片可独立调节圆角 |
| 7 | 设置开关 | 「设置 → 通用 → 导航栏」中新增自定义页面开关 |
| 8 | 导航栏拖拽排序 | 导航栏按钮支持拖拽改变顺序（参考 `GameLauncher`） |

---

## 二、现有代码结构分析

### 2.1 导航栏 (`src/components/ui/sidebar.tsx`)

- **`navItems` 数组**（第306-317行）：静态定义所有导航项，包含 `path`、`icon`、`customIcon`、`ariaLabel`、`beta`
- **可见性控制**：`hideableNavPaths` 数组 + `localStorage` 键 `nexbox_nav_visible_{path}`
- **事件机制**：`nav-visibility-changed` CustomEvent 用于跨组件同步
- **位置切换**：支持左侧 (`left`) 和顶部 (`top`) 两种布局
- **果冻弹跳动画**：状态变化时触发 `jelly-bounce` 动画
- **液态玻璃效果**：通过 `liquidGlassEnabled`、`liquidGlassMode` 控制

### 2.2 搜索索引 (`src/config/search-index.ts`)

- **`SearchCategory` 类型**：`"page" | "builtin-tool" | "optimization" | "thirdparty-tool"`
- **`SearchItem` 接口**：`id`、`nameKey`、`path`、`icon`、`category`、`keywords`、`customIcon`、`action`、`toolId`
- **`searchIndex` 数组**：包含所有页面、内置工具、优化项
- **第三方工具**：运行时从 `useAppStartup().tools` 动态注入，`category === "thirdparty-tool"`
- **可添加到自定义页面的项**：`searchIndex` 中 `category !== "thirdparty-tool"` 的所有项（即 `page`、`builtin-tool`、`optimization`）

### 2.3 快捷启动拖拽参考 (`src/components/GameLauncher.tsx`)

- **库**：`@dnd-kit/core` + `@dnd-kit/sortable`
- **核心组件**：`SortableGameItem`（使用 `useSortable` hook）、`DndContext`、`SortableContext`
- **排序逻辑**：`handleDragEnd` 中使用 `arrayMove` 重新排列
- **拖拽手柄**：`GripVertical` 图标，通过 `{...attributes} {...listeners}` 绑定
- **尺寸调整**：手动 `mousedown/mousemove/mouseup` 实现，支持 `left`、`top`、`corner` 三种边缘
- **持久化**：`localStorage` 存储

### 2.4 设置页面 (`src/pages/SettingsPage.tsx`)

- **`GeneralSettings` 组件**：包含导航栏可见性配置
- **导航项开关**：在 `hideableNavPaths` 数组上映射渲染 `ThemeSwitch`
- **`handleNavItemToggle`**：切换可见性并派发 `nav-visibility-changed` 事件
- **localStorage 键格式**：`nexbox_nav_visible_{path去斜杠和横线}`

### 2.5 路由 (`src/App.tsx`)

- 使用 `react-router-dom` 的 `<Routes>` / `<Route>`
- 每个页面用 `<AnimatedPage>` 包裹实现过渡动画
- 需在两处 `<Routes>` 块中（`pageTransitionMode !== "off"` 和 else 分支）添加路由

### 2.6 卡片组件

- **`LiquidGlassCard`** (`src/components/special/liquid-glass-card.tsx`)：通用液态玻璃卡片
- **`LiquidGlassToolCard`** (`src/components/special/liquid-glass-tool-card.tsx`)：工具卡片，带鼠标光晕效果
- **`ViewItem` 接口** (`src/components/special/view-types.ts`)：`id`、`path`、`icon`、`titleKey`、`descriptionKey`、`color`

---

## 三、技术方案

### 3.1 技术选型

| 能力 | 方案 | 依赖 |
|------|------|------|
| 拖拽排序（导航栏） | `@dnd-kit/core` + `@dnd-kit/sortable` | 已安装 |
| 自由拖拽（画布卡片） | 原生 `mousedown/mousemove/mouseup` | 无额外依赖 |
| 卡片缩放 | 原生 resize 手柄（参考 `GameLauncher`） | 无额外依赖 |
| 状态持久化 | `localStorage` | 无额外依赖 |
| UI 组件 | Chakra UI + `LiquidGlassCard` | 已安装 |
| 国际化 | `react-i18next` | 已安装 |

### 3.2 数据结构设计

```typescript
// src/types/custom-dashboard.ts

/** 自定义页面上的卡片实例 */
export interface CustomCardInstance {
  /** 唯一实例 ID（允许同一工具添加多次） */
  instanceId: string;
  /** 对应 searchIndex 中的 item id */
  itemId: string;
  /** 在画布上的位置 */
  position: { x: number; y: number };
  /** 卡片尺寸 */
  size: { width: number; height: number };
  /** 圆角半径 (px) */
  borderRadius: number;
  /** 是否显示标题 */
  showTitle: boolean;
}

/** 自定义页面的完整配置 */
export interface CustomDashboardConfig {
  /** 所有卡片实例 */
  cards: CustomCardInstance[];
  /** 画布背景模式 */
  backgroundMode: "transparent" | "grid" | "dots";
}

/** 导航栏排序后的路径数组 */
export type NavOrder = string[];
```

### 3.3 localStorage 键设计

| 键名 | 内容 | 默认值 |
|------|------|--------|
| `nexbox_custom_dashboard_config` | `CustomDashboardConfig` JSON | `{ cards: [], backgroundMode: "grid" }` |
| `nexbox_nav_visible__custom` | 自定义页面导航栏可见性 | `"false"`（默认关闭） |
| `nexbox_nav_order` | 导航栏排序数组 | `null`（使用默认顺序） |

---

## 四、详细开发步骤

### 阶段一：自定义页面基础框架

#### 4.1 创建类型定义文件

**文件**：`src/types/custom-dashboard.ts`

定义 `CustomCardInstance`、`CustomDashboardConfig`、`NavOrder` 类型（见 3.2 节）。

#### 4.2 创建自定义页面组件

**文件**：`src/pages/CustomPage.tsx`

**核心结构**：

```
CustomPage
├── 顶部工具栏
│   ├── 页面标题
│   ├── 添加按钮（打开弹窗）
│   ├── 背景模式切换
│   └── 编辑模式切换
├── 画布区域（可滚动）
│   ├── 背景网格/点阵
│   └── 卡片实例列表
│       └── CustomCard（可拖动、可缩放、可调圆角）
└── 添加工具弹窗 AddToolModal
    ├── 搜索框
    └── 卡片网格（图标+名称）
```

**画布实现要点**：

1. **画布容器**：`position: relative`，`overflow: auto`，最小尺寸铺满视口
2. **卡片定位**：`position: absolute`，`left/top` 由 `position` 控制
3. **背景模式**：
   - `grid`：CSS `background-image` 线性渐变网格
   - `dots`：CSS `background-image` 径向渐变点阵
   - `transparent`：无背景

**卡片拖动实现**（参考 `GameLauncher` 的 resize 逻辑）：

```typescript
const onDragStart = (e: React.MouseEvent, instanceId: string) => {
  e.preventDefault();
  const card = cards.find(c => c.instanceId === instanceId);
  if (!card) return;
  
  const startX = e.clientX;
  const startY = e.clientY;
  const startPos = { ...card.position };
  
  const onMove = (ev: MouseEvent) => {
    const dx = ev.clientX - startX;
    const dy = ev.clientY - startY;
    updateCard(instanceId, {
      position: {
        x: Math.max(0, startPos.x + dx),
        y: Math.max(0, startPos.y + dy),
      }
    });
  };
  
  const onUp = () => {
    document.removeEventListener("mousemove", onMove);
    document.removeEventListener("mouseup", onUp);
  };
  
  document.addEventListener("mousemove", onMove);
  document.addEventListener("mouseup", onUp);
};
```

**卡片缩放实现**（右下角拖拽手柄）：

```typescript
const onResizeStart = (e: React.MouseEvent, instanceId: string) => {
  e.preventDefault();
  e.stopPropagation();
  const card = cards.find(c => c.instanceId === instanceId);
  if (!card) return;
  
  const startX = e.clientX;
  const startY = e.clientY;
  const startSize = { ...card.size };
  
  const MIN_W = 120, MIN_H = 100;
  const MAX_W = 500, MAX_H = 500;
  
  const onMove = (ev: MouseEvent) => {
    const dx = ev.clientX - startX;
    const dy = ev.clientY - startY;
    updateCard(instanceId, {
      size: {
        width: Math.min(MAX_W, Math.max(MIN_W, startSize.width + dx)),
        height: Math.min(MAX_H, Math.max(MIN_H, startSize.height + dy)),
      }
    });
  };
  // ... 同上
};
```

**圆角调节**：卡片右上角小按钮，点击弹出 `Slider` 调节 `borderRadius`（0-24px）。

#### 4.3 创建添加工具弹窗

**文件**：`src/components/AddToolModal.tsx`（或内联在 `CustomPage` 中）

**实现要点**：

1. 从 `searchIndex` 过滤 `category !== "thirdparty-tool"` 的项
2. 使用 `Modal` + `SimpleGrid` 平铺展示
3. 每个卡片：小圆角矩形，上方图标，下方名称
4. 顶部搜索框实时过滤
5. 点击卡片 → 在画布上创建新 `CustomCardInstance`，位置默认居中或依次偏移
6. 已添加的项可显示「已添加」标记，但仍允许再次添加（支持多个实例）

**弹窗卡片样式**：

```tsx
<VStack
  spacing={2}
  p={3}
  borderRadius="lg"
  cursor="pointer"
  bg={cardBg}
  border="1px solid"
  borderColor={borderColor}
  _hover={{ bg: hoverBg, transform: "translateY(-2px)" }}
  transition="all 0.2s"
  onClick={() => handleAdd(item)}
>
  <Icon as={item.icon} boxSize={6} color={iconColor} />
  <Text fontSize="xs" fontWeight="medium" noOfLines={1}>
    {t(item.nameKey)}
  </Text>
</VStack>
```

#### 4.4 卡片点击行为

卡片点击后导航到对应路径：

```typescript
const handleCardClick = (card: CustomCardInstance) => {
  const item = searchIndex.find(i => i.id === card.itemId);
  if (item) {
    navigate(item.path);
  }
};
```

### 阶段二：导航栏集成

#### 4.5 导航栏新增自定义页面入口

**文件**：`src/components/ui/sidebar.tsx`

**修改点**：

1. 在 `navItems` 数组中新增条目（放在 `/mood` 和 `/settings` 之间）：

```typescript
import { LayoutGrid } from "lucide-react";

// navItems 数组中新增：
{ path: "/custom", icon: LayoutGrid, ariaLabel: t("sidebar.custom") },
```

2. 在 `hideableNavPaths` 数组中新增 `"/custom"`：

```typescript
const hideableNavPaths = ["/hardware", "/tools", "/builtin-tools", "/optimization", "/music", "/delta-force", "/epic-free", "/mood", "/custom"];
```

3. 默认关闭：确保 `localStorage` 中 `nexbox_nav_visible__custom` 默认为 `"false"`

   > 注意：当前 `navVisibility` 初始化逻辑为 `localStorage.getItem(key) !== "false"`，即默认显示。需要为 `/custom` 特殊处理为默认隐藏：

```typescript
const [navVisibility, setNavVisibility] = useState<Record<string, boolean>>(() => {
  const initial: Record<string, boolean> = {};
  for (const path of hideableNavPaths) {
    const key = getNavStorageKey(path);
    // /custom 默认隐藏，其他默认显示
    if (path === "/custom") {
      initial[path] = localStorage.getItem(key) === "true";
    } else {
      initial[path] = localStorage.getItem(key) !== "false";
    }
  }
  return initial;
});
```

#### 4.6 路由注册

**文件**：`src/App.tsx`

在两处 `<Routes>` 块中添加：

```tsx
import CustomPage from "./pages/CustomPage";

// 在 <Routes> 中新增：
<Route path="/custom" element={<AnimatedPage><CustomPage /></AnimatedPage>} />
```

#### 4.7 国际化

**文件**：`src/locales/zh.json`、`en.json`、`ja.json`、`de.json`、`fr.json`、`zh-TW.json`

新增翻译键：

```json
{
  "sidebar": {
    "custom": "自定义"
  },
  "customPage": {
    "title": "自定义页面",
    "addTool": "添加工具",
    "emptyHint": "点击右上角添加按钮，添加你常用的工具",
    "searchTools": "搜索工具...",
    "removeCard": "移除",
    "adjustRadius": "调整圆角",
    "borderRadius": "圆角",
    "backgroundMode": "背景模式",
    "grid": "网格",
    "dots": "点阵",
    "transparent": "透明",
    "alreadyAdded": "已添加",
    "clearAll": "清空全部",
    "clearAllConfirm": "确定要清空所有卡片吗？"
  },
  "settings": {
    "generalSettings": {
      "customPage": "自定义页面"
    }
  }
}
```

### 阶段三：设置页面开关

#### 4.8 设置页面新增自定义页面开关

**文件**：`src/pages/SettingsPage.tsx`

**修改点**：

1. 在 `GeneralSettings` 组件的导航栏可见性列表中新增 `/custom`：

```typescript
// 在现有的导航项数组中新增：
{ path: "/custom", label: t("sidebar.custom") },
```

2. 同步修改 `navVisibility` 初始化逻辑（与 sidebar 一致，默认隐藏）：

```typescript
const visibilityMap: Record<string, boolean> = {};
for (const p of ["/hardware", "/tools", "/builtin-tools", "/optimization", "/music", "/delta-force", "/epic-free", "/mood", "/custom"]) {
  const key = `nexbox_nav_visible_${p.replace(/\//g, "").replace(/-/g, "_")}`;
  if (p === "/custom") {
    visibilityMap[p] = localStorage.getItem(key) === "true";
  } else {
    visibilityMap[p] = localStorage.getItem(key) !== "false";
  }
}
setNavVisibility(visibilityMap);
```

### 阶段四：导航栏拖拽排序

#### 4.9 导航栏拖拽排序实现

**文件**：`src/components/ui/sidebar.tsx`

**核心改造**：

1. **引入 `@dnd-kit`**：

```typescript
import {
  DndContext,
  closestCenter,
  PointerSensor,
  useSensor,
  useSensors,
  DragEndEvent,
} from "@dnd-kit/core";
import {
  arrayMove,
  SortableContext,
  useSortable,
  horizontalListSortingStrategy,
} from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";
```

2. **创建 `SortableNavButton`**：将现有 `NavButton` 包装为可排序项

```typescript
function SortableNavButton({ item, ...props }: NavButtonProps & { item: NavItem }) {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id: item.path });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.8 : 1,
    zIndex: isDragging ? 10 : 1,
  };

  return (
    <Box ref={setNodeRef} style={style} {...attributes} {...listeners}>
      <NavButton item={item} {...props} />
    </Box>
  );
}
```

3. **持久化排序**：

```typescript
const NAV_ORDER_KEY = "nexbox_nav_order";

const [navOrder, setNavOrder] = useState<string[] | null>(() => {
  const saved = localStorage.getItem(NAV_ORDER_KEY);
  return saved ? JSON.parse(saved) : null;
});

// 根据 navOrder 排序 navItems
const sortedNavItems = useMemo(() => {
  if (!navOrder) return navItems;
  const map = new Map(navItems.map(i => [i.path, i]));
  const ordered = navOrder.map(p => map.get(p)).filter(Boolean) as NavItem[];
  // 追加未在 order 中的新项
  navItems.forEach(item => {
    if (!ordered.find(i => i.path === item.path)) {
      ordered.push(item);
    }
  });
  return ordered;
}, [navOrder, navItems]);

const handleNavDragEnd = (event: DragEndEvent) => {
  const { active, over } = event;
  if (!over || active.id === over.id) return;
  
  const oldIndex = sortedNavItems.findIndex(i => i.path === active.id);
  const newIndex = sortedNavItems.findIndex(i => i.path === over.id);
  if (oldIndex === -1 || newIndex === -1) return;
  
  const newOrder = arrayMove(sortedNavItems, oldIndex, newIndex).map(i => i.path);
  setNavOrder(newOrder);
  localStorage.setItem(NAV_ORDER_KEY, JSON.stringify(newOrder));
};
```

4. **注意**：`/`（首页）和 `/settings`（设置）固定在首尾，不参与排序。通过约束 `useSortable` 的 `disabled` 属性实现：

```typescript
// 在 SortableNavButton 中：
const isFixed = item.path === "/" || item.path === "/settings";
useSortable({ id: item.path, disabled: isFixed });
```

5. **替换渲染**：将 `sidebarContent` 中的直接 `.map()` 改为 `DndContext` + `SortableContext`：

```tsx
<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleNavDragEnd}>
  <SortableContext items={sortedNavItems.map(i => i.path)} strategy={horizontalListSortingStrategy}>
    {sortedNavItems.filter(...).map(item => (
      <SortableNavButton key={item.path} item={item} {...navButtonProps} />
    ))}
  </SortableContext>
</DndContext>
```

6. **拖拽手柄**：为避免与导航点击冲突，使用 `PointerSensor` 的 `activationConstraint: { distance: 8 }`（与 `GameLauncher` 一致），确保只有移动超过 8px 才触发拖拽。

### 阶段五：细节打磨

#### 4.10 卡片右键菜单 / 操作按钮

每个卡片 hover 时显示操作按钮组：

- **拖动手柄**（`GripVertical` 图标）：用于拖动卡片
- **圆角调节**（`CornerStyle` 图标）：点击弹出 Slider
- **删除**（`X` 图标）：移除卡片

#### 4.11 空状态

画布无卡片时显示引导文案和添加按钮：

```tsx
{cards.length === 0 && (
  <Flex direction="column" align="center" justify="center" h="100%">
    <Icon as={LayoutGrid} boxSize={12} color={descColor} mb={4} />
    <Text color={descColor} mb={4}>{t("customPage.emptyHint")}</Text>
    <LiquidGlassButton onClick={openAddModal} leftIcon={<Plus size={16} />}>
      {t("customPage.addTool")}
    </LiquidGlassButton>
  </Flex>
)}
```

#### 4.12 卡片预览内容（可选增强）

卡片内显示对应工具的图标和名称，点击跳转到对应页面。可考虑未来支持内嵌 mini widget。

#### 4.13 响应式适配

- 画布最小宽高保证可用性
- 卡片尺寸限制：最小 `120×100`，最大 `500×500`
- 位置不允许超出画布边界（拖动时 clamp）

#### 4.14 液态玻璃适配

自定义页面和弹窗应使用 `LiquidGlassCard` 保持视觉一致性，背景效果跟随全局设置。

---

## 五、文件变更清单

### 新增文件

| 文件路径 | 说明 |
|----------|------|
| `src/pages/CustomPage.tsx` | 自定义页面主组件 |
| `src/types/custom-dashboard.ts` | 类型定义 |
| `src/components/AddToolModal.tsx` | 添加工具弹窗组件（可选独立） |

### 修改文件

| 文件路径 | 修改内容 |
|----------|----------|
| `src/components/ui/sidebar.tsx` | 新增 `/custom` 导航项；默认隐藏逻辑；导航栏拖拽排序 |
| `src/App.tsx` | 新增 `/custom` 路由 |
| `src/pages/SettingsPage.tsx` | 导航栏可见性列表新增 `/custom`；默认隐藏逻辑 |
| `src/locales/zh.json` | 新增 `sidebar.custom`、`customPage.*` 翻译 |
| `src/locales/en.json` | 同上 |
| `src/locales/ja.json` | 同上 |
| `src/locales/de.json` | 同上 |
| `src/locales/fr.json` | 同上 |
| `src/locales/zh-TW.json` | 同上 |

### 依赖检查

- `@dnd-kit/core`：✅ 已安装（`GameLauncher` 在用）
- `@dnd-kit/sortable`：✅ 已安装
- `@dnd-kit/utilities`：✅ 已安装
- `lucide-react`：✅ 已安装（新增 `LayoutGrid` 图标）

---

## 六、开发顺序建议

```
阶段一（核心页面）
  4.1 类型定义
  → 4.2 CustomPage 基础框架（画布 + 空状态）
  → 4.3 AddToolModal 弹窗
  → 4.4 卡片拖动 + 缩放 + 圆角调节
  → 4.5 持久化（localStorage）

阶段二（导航栏集成）
  4.6 路由注册
  → 4.7 导航栏入口 + 默认隐藏
  → 4.8 国际化

阶段三（设置开关）
  4.9 SettingsPage 新增开关

阶段四（导航栏拖拽排序）
  4.10 sidebar.tsx 改造为 @dnd-kit
  → 4.11 持久化排序

阶段五（打磨）
  4.12 卡片操作按钮
  → 4.13 空状态优化
  → 4.14 响应式 + 液态玻璃适配
```

---

## 七、风险与注意事项

1. **导航栏拖拽与点击冲突**：使用 `activationConstraint: { distance: 8 }` 确保短按为点击，拖拽需移动超过阈值。`/` 和 `/settings` 固定位置不参与排序。

2. **导航栏布局适配**：拖拽排序需同时兼容 `left`（垂直排列）和 `top`（水平排列）两种位置。`SortableContext` 的 `strategy` 需根据 `navPosition` 动态切换：
   - `left` → `verticalListSortingStrategy`
   - `top` → `horizontalListSortingStrategy`

3. **默认隐藏逻辑一致性**：`/custom` 的默认隐藏需在 `sidebar.tsx` 和 `SettingsPage.tsx` 两处同步修改，否则会出现状态不一致。

4. **卡片实例 vs 工具项**：允许同一工具添加多个卡片实例（如两个滤镜卡片），`instanceId` 独立于 `itemId`。

5. **画布滚动**：当卡片拖到画布边缘时，需考虑自动扩展画布或自动滚动。初版可先 clamp 到可见区域。

6. **性能**：卡片数量较多时，拖拽/缩放过程中频繁更新 state 可能导致卡顿。可使用 `requestAnimationFrame` 节流或 `useRef` + 直接 DOM 操作优化。

7. **搜索索引变更兼容**：如果后续 `searchIndex` 新增或删除项，已保存的卡片 `itemId` 可能失效。加载时需过滤无效项。
